theme: "hugo-theme-hello-bulma"
pagination:
  pagerSize: 5
ignoreErrors: ["error-remote-getjson"]
# robots.txt
enableRobotsTXT: true

module:
  mounts:
    - source: content
      target: content
      excludeFiles:
        - "templates/**"
